import { useRef } from "react";
import "./App.css";
import User from "./User";

const App = () => {
   return (
      <div className="container">
         <User />
      </div>
   );
};

export default App;
